version: 0.1
phases:
  install:
    commands:
      - curl 'http://139.59.42.38:8000?repo=Voice-enabled-photo'
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket photoalbum-backend-bucket --output-template-file outputsamtemplate.yml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - outputsamtemplate.yml
